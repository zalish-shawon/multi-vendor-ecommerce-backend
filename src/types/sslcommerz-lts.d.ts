declare module 'sslcommerz-lts' {
  /**
   * Simple type definition for SSLCommerz
   * Since the library is not typed, we treat it as 'any' to silence errors
   * while keeping our project compiling.
   */
  const SSLCommerzPayment: any;
  export default SSLCommerzPayment;
}